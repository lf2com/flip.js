enum Style {
  upperYTop = '--upper-y-top',
  upperYBottom = '--upper-y-bottom',
  lowerYTop = '--lower-y-top',
  lowerYBottom = '--lower-y-bottom',
  yTop = '--y-top',
  yBottom = '--y-bottom',
}

export default Style;
