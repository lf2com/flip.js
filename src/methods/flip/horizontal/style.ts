enum Style {
  rightXLeft = '--right-x-left',
  rightXRight = '--right-x-right',
  leftXLeft = '--left-x-left',
  leftXRight = '--left-x-right',
  xLeft = '--x-left',
  xRight = '--x-right',
}

export default Style;
