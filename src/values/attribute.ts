enum Attribute {
  index = 'index',
  value = 'value',
  mode = 'mode',
  direct = 'direct',
  duration = 'duration',
  direction = 'direction',
  minFlips = 'min-flips',
  maxFlips = 'max-flips',
  perspective = 'perspective',
}

export default Attribute;
