enum Attributes {
  index = 'index',
  value = 'value',
  mode = 'mode',
  duration = 'duration',
  direction = 'direction',
  minFlips = 'min-flips',
  maxFlips = 'max-flips',
}

export default Attributes;
